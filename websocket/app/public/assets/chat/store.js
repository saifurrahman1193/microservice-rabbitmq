let username = '';

// Getter method
const getUsername = () => {
    return username;
}

// Setter method
const setUsername = (name) => {
    username = name;
}

export default {
    getUsername,
    setUsername
}